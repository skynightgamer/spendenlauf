# Pinned to the tested versions for reproducible prod installs. A fresh install
# months later must not pull a breaking FastAPI/Pydantic. Refresh with:
#   ./venv/bin/pip freeze
fastapi==0.138.0
uvicorn[standard]==0.49.0
# Pinned explicitly (not just as transitive deps) because these are the usual
# source of breaking changes for a FastAPI app.
pydantic==2.13.4
starlette==1.3.1
# Used only by simulate.py (dev/testing), harmless in prod.
requests==2.34.2
